{
  "name": "crdt-collab-editor",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared",
    "executor"
  ],
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "dev:executor": "npm run dev -w executor",
    "test": "npm run test -w shared",
    "test:executor": "npm run test -w executor"
  },
  "engines": {
    "node": ">=20"
  }
}
